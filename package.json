{
  "name": "rama-platform-sw-tests",
  "version": "0.0.1",
  "private": true,
  "description": "Service Worker Hash-Sync Tests (§2.12)",
  "scripts": {
    "test": "jest --verbose",
    "test:watch": "jest --watch"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0"
  }
}
